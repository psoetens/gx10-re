# catalogs/ follow-ups + doc banners

**Date:** 2026-05-22
**Plan:** `~/.claude/plans/luminous-tickling-giraffe.md`
**Branch:** main (uncommitted — sits on top of the previous uncommitted catalogs-promotion diff)

## Testable end-goal (quoted from plan §1)

> 1. `ls catalogs/` shows the existing four JSONs **plus** `fx_type_enum.json` and `menu_register_catalog.json` (and the existing `README.md`).
> 2. `python -c "import json; d = json.load(open('catalogs/fx_type_enum.json')); assert len(d) == 83 and d['2'] == 'AIRD PREAMP'"` passes.
> 3. `python -c "import json; d = json.load(open('catalogs/menu_register_catalog.json')); assert 'SystemCommon' in next(iter(d))"` passes.
> 4. `ls captures/preset_memory_names.json captures/user_memory_names.json 2>&1 | grep -c 'No such file'` returns `2`.
> 5. `head -5 docs/firmware_versions.md` and `head -5 docs/gaps.md` each show a one-line "Last updated YYYY-MM-DD — …" banner.
> 6. `grep -rn 'captures/menu_catalog\.json' tools/ catalogs/` returns no matches outside the moved file's old name in unrelated session reports.
> 7. `python tools/validate_catalogs.py` import-check passes (path constant updated).

## Evidence

### 1. `ls catalogs/`

```
README.md
assign_target_table.json
bts_effect_catalog_complete.json
firmware_overlay.json
fx_type_enum.json
menu_register_catalog.json
per_effect_types.json
```

✅ Six JSONs + README, matching plan §1.

### 2. `fx_type_enum.json` loads with 83 entries and `0x02 == 'AIRD PREAMP'`

```
83 entries; 0x02 = 'AIRD PREAMP'; 0x52 = 'AUTO WAH'
```

✅ Generated via `python -c "from fx_type_enum import FX_TYPE_NAME; ..."` — keys are stringified ints (`"0".."82"`) so the table is canonical across languages. Sample at `0x52` (82 decimal) confirms the final entry is `AUTO WAH` per the v2 chart.

### 3. `menu_register_catalog.json` first key contains `SystemCommon`

```
first key: '0x00000000 SystemCommon'; total regions: 14
```

✅

### 4. Memory-names captures deleted

```
ls: cannot access 'captures/preset_memory_names.json': No such file or directory
ls: cannot access 'captures/user_memory_names.json': No such file or directory
```

✅ Both removed via `git rm`. Probe tools (`probe_preset_names.py`, `probe_user_memory_names*.py`, `dump_preset_names.py`) keep their `captures/…` default `--out` paths — the user regenerates per-device on demand. Three doc lines reworded to make the per-device framing explicit (see "What was reworded" below).

### 5. Banners on the two docs

```
--- docs/firmware_versions.md ---
> **Last updated 2026-05-14** — adds the
> `(EDITOR_COMMUNICATION_LEVEL, EDITOR_COMMUNICATION_REVISION)`
> capability-fingerprint finding after BTS-for-Mac source inspection;
> supersedes the earlier "no SysEx address reveals firmware version"
> claim.

# GX-10 / GX-100 firmware versions
```

```
--- docs/gaps.md ---
> **Closeout 2026-05-03 — protocol end-to-end verified on a live device.**
> Open items: §6.1 AUTO OFF (un-investigable), §11 sub-types (deferred).
> Per-section ✅/⚠️/🚫 markers below are the closeout audit's status, not
> work-in-progress flags.

# Documentation gaps — what is NOT yet captured
```

✅

### 6. No stray references

`grep -rn 'captures/menu_catalog\.json' tools/ catalogs/ docs/ README.md` returns empty.

### 7. `validate_catalogs.py` import-check

```
validate_catalogs imported cleanly
```

(Done with a small stub for `rtmidi` since the underlying `midi_io` chain pulls that dep, which isn't installed locally — confirmed the new `MENU_CATALOG = ROOT / "catalogs" / "menu_register_catalog.json"` path constant resolves and the module loads.)

## What was built

- **`catalogs/fx_type_enum.json`** (new) — JSON mirror of `tools/fx_type_enum.py`'s `FX_TYPE_NAME` dict, 83 entries, generated by importing the .py to avoid hand-transcription error.
- **`tools/fx_type_enum.py`** — added a 3-line "keep in sync with `catalogs/fx_type_enum.json`" note to the module docstring. Source-of-truth role unchanged (hand-maintained from the MIDI chart).
- **`catalogs/menu_register_catalog.json`** — `git mv` from `captures/menu_catalog.json` (4324 lines, 116 KiB). Renamed to make "non-effect register map" explicit rather than the vaguer "menu".
- **`tools/validate_catalogs.py`** — updated docstring + `MENU_CATALOG` path constant.
- **`tools/build_menu_catalog.py`** — updated docstring + `OUT` path constant.
- **`captures/preset_memory_names.json`**, **`captures/user_memory_names.json`** — deleted via `git rm`.
- **`docs/gaps.md`** — three lines reworded (see below) + closeout banner prepended.
- **`docs/firmware_versions.md`** — last-updated banner prepended.
- **`catalogs/README.md`** — table extended to six rows (added `menu_register_catalog.json`, `fx_type_enum.json`).

## What was reworded (memory-names framing)

| File:line | Before | After |
|---|---|---|
| `docs/gaps.md:259-260` | "all 100 preset names #200..299 captured into `captures/preset_memory_names.json` via `tools/probe_preset_names.py`" | "all 100 preset names #200..299 probed via `tools/probe_preset_names.py` (writes per-device to `captures/preset_memory_names.json`; not bundled with the repo since they're the factory names already present in every unit)" |
| `docs/gaps.md:399-402` | "captured all 198 GX-10 user-memory names in `captures/user_memory_names.json`" | "walks all 198 GX-10 user-memory slots and writes per-device output to `captures/user_memory_names.json` (output is per-user; not bundled in the repo since the names are the connected device's own data)" |
| `docs/gaps.md:511` | "✅ captured · `tools/probe_preset_names.py` → `captures/preset_memory_names.json`" | "✅ flow verified · `tools/probe_preset_names.py` (per-device output, not bundled)" |

The probe tools' `--out` defaults (`captures/...names.json`) were intentionally **kept** — they're the right *output* location for a per-device probe. Anyone running the tool on their own GX-10 produces their own JSON.

## Skipped vs the plan

- Nothing skipped. All 13 sequence steps executed.

## Unverified / discovered

- **`tools/fx_type_enum.py` sync risk**: now flagged as "keep in sync with the .json" in its docstring, but the sync is human-maintained. If someone adds an entry only to the .py and forgets the .json, the two drift. Not solved here (would require either a generator or a CI check). Acceptable risk: the .py has been stable since the initial commit; new entries arrive ~once per firmware family.
- **`tools/validate_catalogs.py` import-check** required stubbing `rtmidi` locally (transitive dep via `midi_io`). The dep itself is a pre-existing environment issue, not a regression. The module-import path-constant resolution is what was being verified.

## Git state at end

Uncommitted, **on top of the prior uncommitted diff** (catalogs promotion + stale-docs purge from the earlier session). Combined `git status` now shows:

- 5 renames (`R` / `RM`)
- 8 deletions (`D`) — 6 prior stale docs + 2 new memory-names captures
- 16 modifications (`M`)
- 4 new untracked files (`??`) — `catalogs/README.md`, `catalogs/fx_type_enum.json`, and the two `reports/catalogs-*.md`

Total: 33 paths touched across the two sessions. No `git commit` invoked — left for human review.
