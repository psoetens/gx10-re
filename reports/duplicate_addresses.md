# Duplicate-address scan

Per-effect, addresses claimed by more than one knob entry. Each row is a phantom-candidate from the BTS bulk-enum probe where two BTS-UI labels resolved to the same FxItem cell.

Source: `captures/bts_effect_catalog.json`
Effects with duplicates: **0**

_No duplicates._